# Credentials — never commit
# (none needed in v2 — config.js is now checked into git)

# Dependencies (for when backend is added)
node_modules/
backend/node_modules/

# Environment files
.env
.env.local
.env.*.local
.env.test

# Build output (for future frontend build step)
frontend/dist/
frontend/build/

# OS files
.DS_Store
Thumbs.db

# Editor directories
.vscode/
.idea/

# Logs
*.log
npm-debug.log*

# other stuff
miscellaneous-do not put in repo/
.claude/settings.local.json
